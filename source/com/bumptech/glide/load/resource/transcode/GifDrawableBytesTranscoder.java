package com.bumptech.glide.load.resource.transcode;

import com.bumptech.glide.load.Options;
import com.bumptech.glide.load.engine.r;
import v3.a;
import z3.c;

public class GifDrawableBytesTranscoder implements c<y3.c, byte[]> {
    public r<byte[]> a(r<y3.c> rVar, Options options) {
        return new a(f4.a.d(rVar.get().c()));
    }
}
