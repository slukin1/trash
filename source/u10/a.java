package u10;

import java.io.File;
import org.apache.commons.io.monitor.FileAlterationObserver;

public interface a {
    void a(File file);

    void b(File file);

    void c(File file);

    void d(FileAlterationObserver fileAlterationObserver);

    void e(File file);

    void f(File file);

    void g(File file);

    void h(FileAlterationObserver fileAlterationObserver);
}
