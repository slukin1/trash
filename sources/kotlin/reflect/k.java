package kotlin.reflect;

import d10.q;
import kotlin.Unit;

public interface k<D, E, V> extends o<D, E, V>, l {

    public interface a<D, E, V> extends h<V>, q<D, E, V, Unit> {
    }

    a<D, E, V> getSetter();
}
