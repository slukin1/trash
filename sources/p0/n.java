package p0;

import androidx.core.util.Consumer;

public interface n {
    void addOnMultiWindowModeChangedListener(Consumer<i> consumer);

    void removeOnMultiWindowModeChangedListener(Consumer<i> consumer);
}
