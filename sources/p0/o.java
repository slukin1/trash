package p0;

import androidx.core.util.Consumer;

public interface o {
    void addOnPictureInPictureModeChangedListener(Consumer<p> consumer);

    void removeOnPictureInPictureModeChangedListener(Consumer<p> consumer);
}
