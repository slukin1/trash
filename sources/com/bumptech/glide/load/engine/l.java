package com.bumptech.glide.load.engine;

import com.bumptech.glide.load.Options;
import java.util.Map;
import n3.b;
import n3.g;

public class l {
    public k a(Object obj, b bVar, int i11, int i12, Map<Class<?>, g<?>> map, Class<?> cls, Class<?> cls2, Options options) {
        return new k(obj, bVar, i11, i12, map, cls, cls2, options);
    }
}
