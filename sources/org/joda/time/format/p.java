package org.joda.time.format;

import java.util.Locale;
import org.joda.time.i;

public interface p {
    int a(i iVar, int i11, Locale locale);

    void b(StringBuffer stringBuffer, i iVar, Locale locale);

    int d(i iVar, Locale locale);
}
